{
  "name": "cue-proxy",
  "version": "1.0.0",
  "description": "Local proxy to forward Anthropic API requests and avoid CORS in Flutter Web",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-fetch": "^2.7.0"
  }
}
